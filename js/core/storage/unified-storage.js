class UnifiedStorage {
	constructor() {
		/*do nothing*/
	}

	set(key, value) {
		throw ("UnifiedStorage: set() is not defined");
	}

	remove(key) {
		throw ("UnifiedStorage: remove() is not defined");
	}

	has(key) {
		throw ("UnifiedStorage: has() is not defined");
	}

	get(key) {
		throw ("UnifiedStorage: get() is not defined");
	}

	clear() {
		throw ("UnifiedStorage: clear() is not defined");
	}
}